/// <reference path="..\..\typings\bundle.d.ts" />
import * as React from 'react';
import {MainComponent} from './MainComponent';

React.render(React.createElement(MainComponent), document.getElementById('main'));
